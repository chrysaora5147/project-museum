# Project Museum

A small static project notebook hosted on Vercel.

## Files

- `index.html` contains the full page markup and styles.
- `project-museum-favicon.svg` is the browser/app icon.

## Deploying

This repository is connected to the Vercel project `project-museum-vercel`. No build command is required.
